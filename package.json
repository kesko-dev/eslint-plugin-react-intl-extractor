{
  "name": "eslint-plugin-react-intl-extractor",
  "version": "0.1.0",
  "description": "ESLint plugin for detecting duplicates and extracting messageIds and defaultMessages from React Intl tags.",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo 'No tests to run. The tests need some work.'"
  },
  "keywords": [
    "eslint",
    "eslintplugin",
    "react",
    "react-intl"
  ],
  "author": "tcmug",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.12.6",
    "@types/eslint": "^6.1.3",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "fs": "0.0.1-security"
  },
  "peerDependencies": {
    "eslint": ">=6.0.0"
  }
}
